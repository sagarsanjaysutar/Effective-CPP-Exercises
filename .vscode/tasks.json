{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "Normal C++ Build",
      "command": "/usr/bin/g++",
      "args": [
        "-fdiagnostics-color=always",
        "-g",
        "${file}",
        "-o",
        "${workspaceFolder}/Output/${fileBasenameNoExtension}.o"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": "build",
      "detail": "Task generated by Debugger."
    },
    {
      "label": "Normal C++ Run",
      "type": "shell",
      "command": "${workspaceFolder}/Output/${fileBasenameNoExtension}.o",
      "options": {
        "cwd": "${fileDirname}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      },
      "dependsOn": ["Normal C++ Build"],
      "problemMatcher": []
    }
  ],
  "version": "2.0.0"
}
